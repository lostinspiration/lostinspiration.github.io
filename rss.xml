<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Lost Inspiration</title><description>A Memoir</description><link>https://www.kichka.dev/</link><language>en</language><item><title>Enterprise Source Control</title><link>https://www.kichka.dev/posts/source-control/</link><guid isPermaLink="true">https://www.kichka.dev/posts/source-control/</guid><description>Trunk-based development with short-lived feature branches</description><pubDate>Tue, 28 Apr 2026 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;Source Control -- Philosophy&lt;/h1&gt;
&lt;h2&gt;Version History&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Small incremental changes get checked in and preserved&lt;/li&gt;
&lt;li&gt;Each complete unit of work should be a check-in point&lt;/li&gt;
&lt;li&gt;Can see changes to the code over time&lt;/li&gt;
&lt;li&gt;Can compare files to see differences&lt;/li&gt;
&lt;li&gt;Can rollback to a previous version if a regression was found or design decisions have changed&lt;/li&gt;
&lt;li&gt;Most of the time you only have a single branch&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Release Control&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Branch per environment [Dev (main) | Stage | Prod]&lt;/li&gt;
&lt;li&gt;Each branch up to Prod is used as a stepping stone in a promotion process&lt;/li&gt;
&lt;li&gt;Should be validated in Dev before moving to Stage&lt;/li&gt;
&lt;li&gt;Should be validated in Stage before moving to Prod&lt;/li&gt;
&lt;li&gt;Never make changes in the Stage and Prod branches directly.&lt;/li&gt;
&lt;li&gt;Cherry picking changes should be avoided if possible&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;When to Feature Flag&lt;/h2&gt;
&lt;p&gt;If changes are expected to not release by the end of the sprint, feature flagging should be your next consideration so the new code path can be released during a normal release cycle and tested separate from the current flow&lt;/p&gt;
&lt;hr /&gt;
&lt;h1&gt;Using Git&lt;/h1&gt;
&lt;h2&gt;Commands&lt;/h2&gt;
&lt;h3&gt;clone&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git clone https://repository.git&lt;/code&gt; -- Creates a local copy of a remote repository, downloading all files, branches, and commit history into a new directory on your machine. This is typically the first command you run when joining and existing project.&lt;/p&gt;
&lt;h3&gt;branch&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git branch&lt;/code&gt; -- Lists all local branches in the repository, marking the currently checked-out branch with an asterisk. Add &lt;code&gt;-a&lt;/code&gt; to include remote branches or &lt;code&gt;-v&lt;/code&gt; to see the latest commit on each.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;git branch branch_name&lt;/code&gt; -- Creates a new branch with the given name pointing at your current commit, but does not switch you to it. Follow with a &lt;code&gt;git switch branch_name&lt;/code&gt; to switch to it.&lt;/p&gt;
&lt;p&gt;:::tip
You can use the command &lt;code&gt;git switch -c branch_name&lt;/code&gt; to create and switch in one operation.
:::&lt;/p&gt;
&lt;p&gt;&lt;code&gt;git branch -D branch_name&lt;/code&gt; -- Force deletes the named local branch, even if it has unmerged changes that would otherwise be lost. Use lowercase &lt;code&gt;-d&lt;/code&gt; instead for a safer delete that blocks if the branch hasn&apos;t been merged.&lt;/p&gt;
&lt;p&gt;:::note
Because we leverage squash merges into main the &lt;code&gt;-d&lt;/code&gt; will always think that there are unmerged changes.
:::&lt;/p&gt;
&lt;h3&gt;switch&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git switch branch_name&lt;/code&gt; -- Switches your working directory to the specified branch, updating your files to match that branch&apos;s latest commit. Add &lt;code&gt;-c&lt;/code&gt; to create and switch to a new branch in one step.&lt;/p&gt;
&lt;h3&gt;status&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git status&lt;/code&gt; -- Shows the current state of your working directory and staging area, including which files have been modified, staged for commit, or are untracked. It&apos;s the go-to command for checking where you stand before staging, committing or switching branches.&lt;/p&gt;
&lt;h3&gt;add&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git add .&lt;/code&gt; -- Stages all new and modified files in the current directory (and its subdirectories) for the next commit. Use with care, it can be quick to sweep in files you didn&apos;t intend to include, so a &lt;code&gt;git status&lt;/code&gt; check before hand is a good habit.&lt;/p&gt;
&lt;p&gt;:::tip
A clean &lt;code&gt;.gitignore&lt;/code&gt; will be your first line of defense. If &lt;code&gt;git status&lt;/code&gt; is consistently showing files you don&apos;t want to track, node_modules/, build artifacts, .env files, etc., add them to &lt;code&gt;.gitignore&lt;/code&gt;.
:::&lt;/p&gt;
&lt;h3&gt;commit&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git commit -m &quot;message&quot;&lt;/code&gt; -- Records your staged changes to the local repository with a short inline message describing what changed.&lt;/p&gt;
&lt;p&gt;:::tip
Keep messages clear and meaningful (e.g., &quot;fix login redirect bug&quot; rather than &quot;stuff&quot;)&lt;/p&gt;
&lt;p&gt;For anything needing more detail, omit the &lt;code&gt;-m&lt;/code&gt; flag to open your editor and write a full multi-line message with a subject line and body.
:::&lt;/p&gt;
&lt;h3&gt;push&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git push --set-upstream origin branch_name&lt;/code&gt; -- Pushes a local branch to the remote (origin) for the first time and links it to a remote tracking branch of the same name, so future &lt;code&gt;git push&lt;/code&gt; and &lt;code&gt;git pull&lt;/code&gt; commands work without specifying the remote and branch.&lt;/p&gt;
&lt;p&gt;:::tip
To skip this entirely on new branches, set &lt;code&gt;git config --global push.autoSetupRemote true&lt;/code&gt;. With this enabled, a plain &lt;code&gt;git push&lt;/code&gt; on a brand new local branch will automatically create the matching upstream branch on origin and link them, so you do not have to remember the &lt;code&gt;--set-upstream&lt;/code&gt; flag.
:::&lt;/p&gt;
&lt;h3&gt;pull&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git pull&lt;/code&gt; -- Fetches the latest changes from the remote branch and merges them into your current local branch in one step. Run it before starting new work and before pushing to minimize merge conflicts.&lt;/p&gt;
&lt;p&gt;:::note
Under the hood it&apos;s a &lt;code&gt;git fetch&lt;/code&gt; followed by a &lt;code&gt;git merge&lt;/code&gt;.
:::&lt;/p&gt;
&lt;h3&gt;restore&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git restore file_name&lt;/code&gt; -- Discards local changes to the specified file, restoring it to match the latest commit on your current branch. Add &lt;code&gt;--staged&lt;/code&gt; to unstage a file without losing your edits, making it the modern replacement for &lt;code&gt;git reset HEAD file_name&lt;/code&gt;. You can also combine it with &lt;code&gt;--staged --worktree&lt;/code&gt; to fully revert a staged change.&lt;/p&gt;
&lt;p&gt;:::note
You&apos;ll often see &lt;code&gt;git checkout&lt;/code&gt; used for both branch switching and file restoration in older documentation, tutorials and forums. This double duty has caused enough pain that &lt;code&gt;git switch&lt;/code&gt; and &lt;code&gt;git restore&lt;/code&gt; were later introduced as safer more explicit alternatives.
:::&lt;/p&gt;
&lt;h3&gt;fetch&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;git fetch --prune&lt;/code&gt; -- Downloads the latest refs and objects from the remote and simultaneously removes any remote-tracking branches in your local repo whose upstream counterparts no longer exist. This is a great housekeeping command to run periodically so &lt;code&gt;git branch -a&lt;/code&gt; doesn&apos;t accumulate stale references to branches that have already been deleted on the remote.&lt;/p&gt;
&lt;hr /&gt;
&lt;h1&gt;Leveraging .gitignore&lt;/h1&gt;
&lt;p&gt;Set this file up early, preferably before your first commit. Once a file is tracked, adding it to &lt;code&gt;.gitignore&lt;/code&gt; will not untrack it. You&apos;ll need to &lt;code&gt;git rm --cached file_name&lt;/code&gt; (&lt;em&gt;which removes it from the repo but keeps it locally&lt;/em&gt;) followed by a commit. Catching this early avoids the need for cleanups later.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# visual studio nonsense
.vs/
*.suo
*.user
*.userosscache
*.csproj.user
*.vspscc
*.dbmdl
*.jfm

# artifacts
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
[Dd]ist/
[Ll]ogs/
[Pp]ublish/

[Tt]est[Rr]esult*/

[Pp]ackages/
*.nupkg

node_modules/

*.publishsettings
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;:::tip
Run &lt;code&gt;git status --ignored&lt;/code&gt; periodically to see what&apos;s actually being ignored.
:::&lt;/p&gt;
&lt;hr /&gt;
&lt;h1&gt;Local Git Setup&lt;/h1&gt;
&lt;p&gt;Because we will be leveraging feature branches for getting code into main, it will be common practice to push your feature branch to the server for a PR and review. Applying the following setting will make this practice easier and less verbose.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;git config --global push.autoSetupRemote true&lt;/code&gt;&lt;/p&gt;
&lt;hr /&gt;
&lt;h1&gt;Developer Workflow&lt;/h1&gt;
&lt;h2&gt;Feature Coding&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Get the repository (&lt;em&gt;naturally skip this step if you already have it&lt;/em&gt;)&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git clone https://repository.git
cd repository_name
&lt;/code&gt;&lt;/pre&gt;
&lt;ol&gt;
&lt;li&gt;Get the latest version and create a new branch&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git switch main
git pull
git switch -c feature/cool-feature
&lt;/code&gt;&lt;/pre&gt;
&lt;ol&gt;
&lt;li&gt;Work and commit loop&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git status
git add .
git commit -m &quot;descriptive commit message&quot;
&lt;/code&gt;&lt;/pre&gt;
&lt;ol&gt;
&lt;li&gt;Ready for review / deployment&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git push
&lt;/code&gt;&lt;/pre&gt;
&lt;ol&gt;
&lt;li&gt;Navigate to GitHub and open a pull request using your new branch&lt;/li&gt;
&lt;li&gt;Get review and perform a &lt;code&gt;squash merge&lt;/code&gt; when approved. (&lt;em&gt;Once the PR is approved the branch on the server will be automatically cleaned up&lt;/em&gt;)&lt;/li&gt;
&lt;li&gt;Reset for next feature / cleanup&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git switch main
git pull
git branch -D feature/cool-feature
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Long Running Branches&lt;/h2&gt;
&lt;p&gt;If a feature branch lives more than a day or two, main may have moved forward and you will want to pull those changes in to avoid painful merge later.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;git switch feature/cool-feature
git pull origin main
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Code Promotion&lt;/h2&gt;
&lt;p&gt;Code promotion is handled through PRs between &lt;code&gt;main -&amp;gt; stage&lt;/code&gt; and &lt;code&gt;stage -&amp;gt; prod&lt;/code&gt;. With these PRs, the source is enforced such that you cannot accidentally merge from some other branch into stage or prod.&lt;/p&gt;
&lt;p&gt;You typically want to perform a &lt;code&gt;merge commit&lt;/code&gt; so that the git history matches as you move between main, stage, and prod.&lt;/p&gt;
&lt;hr /&gt;
&lt;h1&gt;Git Gotchas!&lt;/h1&gt;
&lt;h2&gt;Changes on main...&lt;/h2&gt;
&lt;p&gt;If you made changes on main only to remember that force pushes have been disabled, you can create a branch with your existing changes the same way you would otherwise, and your existing changes will carry over. From here you can commit and go through the rest of the process.&lt;/p&gt;
&lt;h3&gt;Working on the wrong branch&lt;/h3&gt;
&lt;p&gt;If you have uncomitted changes, create a new branch, make your commit and work as normal. You can then discard any changes on main.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;git switch -c feature/cool-feature
git status
git add .
git commit -m &quot;...&quot;
git push
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;You&apos;re on main with commits that should be on a branch&lt;/h3&gt;
&lt;p&gt;If you already have commits, the trick is to create a new branch, switch back to main and reset back to match the remote.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;git switch -c feature/cool-feature
git switch main
git reset --hard origin/main
git switch feature/cool-feature
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;PR has a merge conflict&lt;/h2&gt;
&lt;p&gt;Someone merged to main after you branched, and now your PR shows conflicts. Resolve them locally on your feature branch.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Get the latest changes into your branch&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git switch feature/cool-feature
git pull origin main
&lt;/code&gt;&lt;/pre&gt;
&lt;ol&gt;
&lt;li&gt;Resolve any conflicts in your editor&lt;/li&gt;
&lt;li&gt;Push them back to the server. The PR will automatically update.&lt;/li&gt;
&lt;/ol&gt;
&lt;pre&gt;&lt;code&gt;git status
git add .
git commit -m &quot;...&quot;
git push
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Need to undo a commit that&apos;s already pushed&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;DO NOT&lt;/strong&gt; &lt;code&gt;git reset&lt;/code&gt; on anything that has been pushed. It rewrites history and will cause problems for anyone else who has pulled. Instead we want to use &lt;code&gt;git revert&lt;/code&gt; instead, which creates a new commit that undoes the changes from a previous one.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;git revert commit_sha
git push
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Where did my changes go? (reflog safety net)&lt;/h2&gt;
&lt;p&gt;If you ever thing you have lost commits due to a bad reset, rebase, or force delete, check the reflog.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;git reflog
git checkout commit_sha
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;:::note
Git keeps a log of every HEAD movement for roughly 90 days, so almost nothing is truly lost as long as the commit existed at some point. Find the SHA from before things went wrong, check it out, and create a branch from it to preserve the work.
:::&lt;/p&gt;
&lt;hr /&gt;
&lt;h1&gt;GitHub Setup&lt;/h1&gt;
&lt;h2&gt;Branch Source Enforcement Workflows&lt;/h2&gt;
&lt;p&gt;Create a repository named &lt;code&gt;gh-workflows&lt;/code&gt;, or a name of your choosing, to store these global workflows.&lt;/p&gt;
&lt;p&gt;Under the repository &lt;code&gt;Settings &amp;gt; Actions &amp;gt; General #Access&lt;/code&gt; select the appropriate access level, either org or enterprise depending on your needs.
This step is critical, otherwise other repositories will not be able to leverage these workflows, in addition to them not being available to select
within the ruleset settings.&lt;/p&gt;
&lt;h3&gt;Workflows&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;enforce-stage-source.yml&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;name: Enforce stage source
on:
  pull_request: {}

jobs:
  check-source:
    runs-on: ubuntu-latest
    steps:
      - name: Validate source branch
        run: |
          if [[ &quot;${{ github.head_ref }}&quot; != &quot;main&quot; ]]; then
            echo &quot;::error::Stage PRs must come from main, not ${{ github.head_ref }}&quot;
            exit 1
          fi
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;enforce-prod-source.yml&lt;/code&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;name: Enforce production source
on:
  pull_request: {}

jobs:
  check-source:
    runs-on: ubuntu-latest
    steps:
      - name: Validate source branch
        run: |
          if [[ &quot;${{ github.head_ref }}&quot; != &quot;stage&quot; ]]; then
            echo &quot;::error::Production PRs must come from stage, not ${{ github.head_ref }}&quot;
            exit 1
          fi
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Rulesets&lt;/h2&gt;
&lt;p&gt;These rulesets can be configured at the org or enterprise level as you see fit.&lt;/p&gt;
&lt;h3&gt;Protect Main Branches&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Target repositories&lt;/strong&gt;: All repositories&lt;br /&gt;
&lt;strong&gt;Target branches&lt;/strong&gt;: Default&lt;br /&gt;
&lt;strong&gt;Restrict deletions&lt;/strong&gt;: Yes&lt;br /&gt;
&lt;strong&gt;Require pull request before merging&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Required approvals&lt;/strong&gt;: 0&lt;br /&gt;
 &lt;strong&gt;Dismiss stale pull request approvals when new commits are pushed&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Allowed merge methods&lt;/strong&gt;: Squash&lt;br /&gt;
&lt;strong&gt;Block force pushes&lt;/strong&gt;: Yes&lt;br /&gt;
&lt;strong&gt;Require code scanning results&lt;/strong&gt;: Yes&lt;/p&gt;
&lt;h3&gt;Protect Stage Branches&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Target repositories&lt;/strong&gt;: All repositories&lt;br /&gt;
&lt;strong&gt;Target branches&lt;/strong&gt;: stage&lt;br /&gt;
&lt;strong&gt;Restrict deletions&lt;/strong&gt;: Yes&lt;br /&gt;
&lt;strong&gt;Require pull request before merging&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Required approvals&lt;/strong&gt;: 0&lt;br /&gt;
 &lt;strong&gt;Dismiss stale pull request approvals when new commits are pushed&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Allowed merge methods&lt;/strong&gt;: Merge&lt;br /&gt;
&lt;strong&gt;Block force pushes&lt;/strong&gt;: Yes&lt;br /&gt;
&lt;strong&gt;Require workflows to pass before merging&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Do not require workflows on creation&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Enforce stage source&lt;/strong&gt;&lt;br /&gt;
  1. Add workflow&lt;br /&gt;
  2. Select &lt;code&gt;gh-workflows&lt;/code&gt; repository&lt;br /&gt;
  3. Select &lt;code&gt;main&lt;/code&gt; branch&lt;br /&gt;
  4. Enter path to &lt;code&gt;enforce-stage-source.yml&lt;/code&gt; file&lt;/p&gt;
&lt;h3&gt;Protect Main Branches&lt;/h3&gt;
&lt;p&gt;&lt;strong&gt;Target repositories&lt;/strong&gt;: All repositories&lt;br /&gt;
&lt;strong&gt;Target branches&lt;/strong&gt;: prod&lt;br /&gt;
&lt;strong&gt;Restrict deletions&lt;/strong&gt;: Yes&lt;br /&gt;
&lt;strong&gt;Require pull request before merging&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Required approvals&lt;/strong&gt;: 0&lt;br /&gt;
 &lt;strong&gt;Dismiss stale pull request approvals when new commits are pushed&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Allowed merge methods&lt;/strong&gt;: Merge&lt;br /&gt;
&lt;strong&gt;Block force pushes&lt;/strong&gt;: Yes&lt;br /&gt;
&lt;strong&gt;Require workflows to pass before merging&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Do not require workflows on creation&lt;/strong&gt;: Yes&lt;br /&gt;
 &lt;strong&gt;Enforce production source&lt;/strong&gt;&lt;br /&gt;
  1. Add workflow&lt;br /&gt;
  2. Select &lt;code&gt;gh-workflows&lt;/code&gt; repository&lt;br /&gt;
  3. Select &lt;code&gt;main&lt;/code&gt; branch&lt;br /&gt;
  4. Enter path to &lt;code&gt;enforce-prod-source.yml&lt;/code&gt; file&lt;/p&gt;
&lt;h2&gt;Repository Level Settings&lt;/h2&gt;
&lt;p&gt;To keep each repository tidy, enable the following setting. It will automatically delete feature branches upon successful
merge of a PR.&lt;/p&gt;
&lt;p&gt;Under &lt;code&gt;Settings &amp;gt; General #Pull Requests&lt;/code&gt;&lt;br /&gt;
&lt;strong&gt;Automatically delete head branches&lt;/strong&gt;: Yes&lt;/p&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>Vibin Dirty – A Vibe Coding Experiment</title><link>https://www.kichka.dev/posts/vibin-dirty/</link><guid isPermaLink="true">https://www.kichka.dev/posts/vibin-dirty/</guid><description>♫ They see me vibin, codin / reviewers tryin to catch me vibin dirty / catch me vibin dirty ♫</description><pubDate>Wed, 25 Mar 2026 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;♫ They see me vibin, codin ♫&lt;br /&gt;
♫ Reviewers tryin to catch me vibin dirty, catch me vibin dirty ♫&lt;/p&gt;
&lt;p&gt;It started out as a normal &quot;&lt;em&gt;Play Sunday&lt;/em&gt;&quot;. Drop kiddo off at the parents, brunch with the wife, see play at Playhouse Square, back to the parents for dinner and to get kiddo.&lt;/p&gt;
&lt;p&gt;On the way to brunch, I was complaining about needing to sign back up for Facebook just to get notifications and communication for the school district which lead into having a billion apps for all the notifications I like to see. Ring and Nextdoor for crime and safety, CodeRed for county alerts, NWS for severe weather. I jokingly said that &quot;&lt;em&gt;I should just vibe code something to aggregate everything&lt;/em&gt;&quot;, to which she responded &quot;&lt;em&gt;You should&lt;/em&gt;&quot;.&lt;/p&gt;
&lt;p&gt;I opened up a chat with Claude Opus 4.6 and got to work. I gave it the premise, the technology I want to use, and started working on a plan document. By the time we got to brunch, the plan was pretty solid and I let it loose to start generating the artifacts.&lt;/p&gt;
&lt;p&gt;This is where the experiment begins. Can I truly vibe code something. No hand holding. Just tell it what to do and go. I was not about to miss out on my day date with the wife wanting to be engaged and present. There were plenty of points to check in on the AI, be it potty breaks, driving, or that the conversation circled back to what it was doing. No other point during our time together was I thinking about what the AI was doing. Just the way I wanted it.&lt;/p&gt;
&lt;h3&gt;The Plan — Scuttlebutt&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;em&gt;The word on the street.&lt;/em&gt; A self-hosted, LLM-powered dashboard and RSS feed that aggregates hyperlocal safety, weather, and community information into a single prioritized stream.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4&gt;1. Goals&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Single pane of glass&lt;/strong&gt; for neighborhood-relevant information&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Priority-scored&lt;/strong&gt; via local LLM (Ollama) — surface critical alerts, suppress noise&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Deduplicated&lt;/strong&gt; across sources (same incident reported on Ring + Nextdoor + Facebook → one event)&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Near-real-time&lt;/strong&gt; for safety/weather; digest-latency acceptable for community posts&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Zero cloud dependencies&lt;/strong&gt; — runs entirely on local hardware&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RSS-first output&lt;/strong&gt; — consumable by any feed reader, automation tool, or push notification service&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Minimal JS&lt;/strong&gt; — server-rendered HTML via HTMX with SSE for live updates&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;2. Sources &amp;amp; Ingestion Strategy&lt;/h4&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Source&lt;/th&gt;
&lt;th&gt;Method&lt;/th&gt;
&lt;th&gt;Latency&lt;/th&gt;
&lt;th&gt;Auth&lt;/th&gt;
&lt;th&gt;Status&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;NWS Severe Weather&lt;/td&gt;
&lt;td&gt;REST API (&lt;code&gt;api.weather.gov&lt;/code&gt;)&lt;/td&gt;
&lt;td&gt;~60s polling&lt;/td&gt;
&lt;td&gt;None&lt;/td&gt;
&lt;td&gt;✅ Done&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;CodeRED Alerts&lt;/td&gt;
&lt;td&gt;IMAP email parsing&lt;/td&gt;
&lt;td&gt;~1–5 min&lt;/td&gt;
&lt;td&gt;Email credentials&lt;/td&gt;
&lt;td&gt;✅ Done&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Ring (device events)&lt;/td&gt;
&lt;td&gt;Node.js bridge (&lt;code&gt;ring-client-api&lt;/code&gt;)&lt;/td&gt;
&lt;td&gt;~5s real-time&lt;/td&gt;
&lt;td&gt;Ring refresh token&lt;/td&gt;
&lt;td&gt;✅ Done&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Ring (Neighbors feed)&lt;/td&gt;
&lt;td&gt;IMAP email notifications&lt;/td&gt;
&lt;td&gt;~5–15 min&lt;/td&gt;
&lt;td&gt;Email credentials&lt;/td&gt;
&lt;td&gt;✅ Done&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Facebook Groups&lt;/td&gt;
&lt;td&gt;IMAP email notifications&lt;/td&gt;
&lt;td&gt;~5–15 min&lt;/td&gt;
&lt;td&gt;Email credentials&lt;/td&gt;
&lt;td&gt;✅ Done&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Nextdoor&lt;/td&gt;
&lt;td&gt;IMAP email digests&lt;/td&gt;
&lt;td&gt;~15–60 min&lt;/td&gt;
&lt;td&gt;Email credentials&lt;/td&gt;
&lt;td&gt;✅ Done&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;By the end of the day, there were enough touch points that we had made it completely through our plan and was ready to check the code.&lt;/p&gt;
&lt;p&gt;Two days later, I finally had the time.&lt;/p&gt;
&lt;h3&gt;The Setup&lt;/h3&gt;
&lt;blockquote&gt;
&lt;p&gt;Follow the instructions to make sure that dependencies were present.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;Check!&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Build the project... err... ok, compilation errors. No big deal, let&apos;s work through them.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;For some reason Opus chose to use libcurl and FFI rather than what is in the Zig standard library. Fine... Whatever. I didn&apos;t have to write the code. It claimed to fix the FFI type issues and the compilation errors went away. Who am I to argue?!?&lt;/p&gt;
&lt;p&gt;It also experienced some language version confusion, deciding to use constructs and syntax from an older version of the language even though I had told it to use the latest version. No big deal. It was able to address those issues easy enough.&lt;/p&gt;
&lt;p&gt;There were many places that were violating the &lt;code&gt;var&lt;/code&gt;, &lt;code&gt;const&lt;/code&gt; and &lt;code&gt;variable usage&lt;/code&gt; rules that Zig enforces at compile time. These were the most obnoxious to fix.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Project compiles. Lets run... err... segfault...&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;The segfault was in the way that it was trying to interface with the libcurl library. After a bit of back and forth of trying to work out the issues, I decided to call it.&lt;/p&gt;
&lt;p&gt;I could have continued to instruct it to remove libcurl and replace with functionality from the standard library but at this point I was curious what other bad decisions were waiting for me.&lt;/p&gt;
&lt;h3&gt;The Review&lt;/h3&gt;
&lt;p&gt;I started to review the code to understand what was going on.&lt;/p&gt;
&lt;p&gt;The first thing I noticed were lots of single line function bodies. Most of them were wrappers around the C FFI function calls but they didn&apos;t even do anything useful like clarify an FFI function name or marshal data across the boundary.&lt;/p&gt;
&lt;p&gt;🤔 &lt;em&gt;I guess one could argue that it was trying to conform to the different function naming semantics between Zig and the C libraries.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Related, there were many functions with only one call site...&lt;/p&gt;
&lt;p&gt;🤔 &lt;em&gt;Maybe it was done for assigning a name to a given unit of work or for testability? But for me, that is a stretch. Inline until otherwise needed.&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Lots of OOPness and other programming paradigms that made the code feel over engineered.&lt;/p&gt;
&lt;p&gt;🤔 &lt;em&gt;I am not claiming that these practices are bad, but I tend to treat them more as guidelines then the rule. Straight out of the gate it feels like premature optimization and makes the code harder to reason about.&lt;/em&gt;&lt;/p&gt;
&lt;h3&gt;What Next?&lt;/h3&gt;
&lt;p&gt;It&apos;s not all doom and gloom. After taking things into my own hands, getting rid of all the libcurl FFI nonsense, replacing it with functionality in the standard library, things were starting to come together according to the initial plan.&lt;/p&gt;
&lt;p&gt;The things that I ended up keeping were the initial plan document, a couple of cool coding patterns, a few small units of work around working with IMAP for reference, some constructs that had me going &quot;&lt;em&gt;Oh cool, I didn&apos;t know you could do that in Zig!&lt;/em&gt;&quot;.&lt;/p&gt;
&lt;p&gt;Everything else was tossed.&lt;/p&gt;
&lt;p&gt;If I had to quantify, it would be somewhere between 80-87% of the generated code was or will be deleted and rewritten.&lt;/p&gt;
&lt;h3&gt;How to Increase Success&lt;/h3&gt;
&lt;p&gt;:::tip
Ground the model in truths
:::&lt;/p&gt;
&lt;p&gt;This is arguably the most important tip to increasing success when working with large language models.&lt;/p&gt;
&lt;p&gt;The language version confusion as well as some of the API usage problems that were encountered could have been solved by providing and grounding the AI with the appropriate documentation. The platforms have the ability to search the web for this information, but at this point in time at least, some places are still very hostile to bots accessing the content.&lt;/p&gt;
&lt;p&gt;This also focuses the model limiting the search space. You can get wildly different results between &quot;&lt;em&gt;build me a kernel driver in rust&lt;/em&gt;&quot; and &quot;&lt;em&gt;build me a kernel driver in rust&lt;/em&gt;&quot; then proceeding to provide the rust language documentation, hardware specification and any programming guidelines required.&lt;/p&gt;
&lt;p&gt;:::tip
More checkpoints and smaller focused units of work
:::
I find that the current models like to &lt;em&gt;computer science&lt;/em&gt; the crap out of whatever it is doing. It takes effort to keep it on track. Remember that every line of code is a liability and something that must be maintained.&lt;/p&gt;
&lt;p&gt;Looking at my experience above, I would have;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Steered it clear of using libcurl in favor of the standard library functionality.&lt;/li&gt;
&lt;li&gt;Pushed it to inline more functionality until I felt it necessary to pull them out. Especially if there was only one callsite, or if there was no FFI marshaling convenience.&lt;/li&gt;
&lt;li&gt;If I was also in an interface where I or it could compile the code as we went along, the version and API confusion could have been caught early as well.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;:::tip
Leverage an agentic workflow
:::&lt;/p&gt;
&lt;p&gt;Spinning up smaller &lt;em&gt;throw away&lt;/em&gt; agents to do the work having the primary model do the verification keeps its context small, clean and relevant. There are many times working in a single session I find that the model is confusing itself with decisions made in the past. This signals to me it&apos;s time to start a new session with only the relevant decision points and none of the back and forth that got us there. This practice prolongs, if not automates that away.&lt;/p&gt;
&lt;h3&gt;Final Thoughts&lt;/h3&gt;
&lt;p&gt;I don&apos;t want to babysit the AI.&lt;/p&gt;
&lt;p&gt;If I am going to &lt;em&gt;vibe&lt;/em&gt;, I want it to take my poorly worded idea and push it across the finish line with some definition of reasonably maintainable code. Something that another human or AI can pickup and go. You know, like how enterprise software development is done today. Right?... RIGHT?!? 😏&lt;/p&gt;
&lt;p&gt;AI should be doing the stuff I &lt;strong&gt;DON&apos;T&lt;/strong&gt; want to do, so that I can be engaged and present in the things that I &lt;strong&gt;DO&lt;/strong&gt; want to do.&lt;/p&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>Deforestation (Logging)</title><link>https://www.kichka.dev/posts/deforestation/</link><guid isPermaLink="true">https://www.kichka.dev/posts/deforestation/</guid><description>Logging — The Stack, Fundamentals, and the hard won experience that has continued to pay dividens. With bonus content of extending it into SQL Server</description><pubDate>Tue, 30 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;Nothing is more frustrating than a system being broken with no visibility into it, at the same time there is nothing more satisfying than catching problems with your vendors before they do. Being proactive, rather than reactive also buys a ton of grace from your users when you catch problems before they are reported and sometime having gone totally unnoticed.&lt;/p&gt;
&lt;p&gt;This reminds me of a quote from the &amp;lt;a href=&quot;https://www.imdb.com/title/tt0756880/&quot; target=&quot;_blank&quot;&amp;gt;Futurama Godfellas&amp;lt;/a&amp;gt; episode where the Galaxy God Entity is talking to Bender and says &lt;em&gt;&quot;When you do things right, people won&apos;t be sure you&apos;ve done anything at all&quot;&lt;/em&gt; and I couldn&apos;t agree more. Proper logging and visibility will lead to exactly that!&lt;/p&gt;
&lt;p&gt;Below we will look at how I have implemented it and the choices made along the way.&lt;/p&gt;
&lt;h1&gt;The Software Stack&lt;/h1&gt;
&lt;h2&gt;&amp;lt;a href=&quot;https://grafana.com/oss/grafana/?pg=get&amp;amp;plcmt=selfmanaged-box1-cta2&quot; target=&quot;_blank&quot;&amp;gt;Grafana&amp;lt;/a&amp;gt;&lt;/h2&gt;
&lt;p&gt;This is the user interface to query logs and metrics, creating rich dashboards and visualizations, and alerting when specific conditions are met.&lt;/p&gt;
&lt;h2&gt;&amp;lt;a href=&quot;https://grafana.com/oss/loki/?pg=get&amp;amp;plcmt=selfmanaged-box2-cta2&quot; target=&quot;_blank&quot;&amp;gt;Loki&amp;lt;/a&amp;gt;&lt;/h2&gt;
&lt;p&gt;This is a platform specifically optimized for handling message streams. I prefer to instrument my applications using JSON messages that Grafana can then parse out with &amp;lt;a href=&quot;https://grafana.com/docs/loki/latest/query/log_queries/&quot; target=&quot;_blank&quot;&amp;gt;LogQL&amp;lt;/a&amp;gt;. Become familiar with writing log queries as that is how you will explore the data.&lt;/p&gt;
&lt;p&gt;Log streams are grouped by labels. These labels have served me quite well for many years.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Application&lt;/strong&gt; is the top level assembly or application that produced the log&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Environment&lt;/strong&gt; is the environment &lt;em&gt;(e.g. dev, stage, production)&lt;/em&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;MachineName&lt;/strong&gt; is the computer or server running the application
:::warning
Loki will group and store in like chunks (files) on the server using the stream labels. You want this to be as &lt;strong&gt;low cardinality&lt;/strong&gt; as possible to avoid having too many individual files on the server.
:::&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;You might be asking at this point, &quot;How do I add additional information to my log messages&quot;? This is where the Log Property Stack comes in. It is a way to enrich your logs with additional context without trying to jam it into the log message itself.&lt;/p&gt;
&lt;h4&gt;Log Property Stack&lt;/h4&gt;
&lt;p&gt;Instead of adding all the details to the log message itself, you should utilize the property stack which will persist the property with every message nested within the scope that property was defined.&lt;/p&gt;
&lt;p&gt;Here are some examples of what I use through my applications. These are staples that I include in everything and add more as needed to determine application state at major branching points.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;InstanceId&lt;/strong&gt; for long running process, defering to CorrelationId for nested units of work&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;CorrelationId&lt;/strong&gt; for discrete units of work&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;RequestInfo&lt;/strong&gt; object for web stuff like url, headers, query params, and other information to help trace requests.&lt;/li&gt;
&lt;/ul&gt;
&lt;h4&gt;Dependency Recommendations&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Serilog&lt;/strong&gt; for dotnet applications&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Serilog.Sinks.Debug&lt;/strong&gt; for web applications when you are running locally&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Serilog.Sinks.Console&lt;/strong&gt; for console applications writing to stdout/stderr&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Serilog.Sinks.Grafana.Loki&lt;/strong&gt; for writing to a Loki server&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SerilogTimings&lt;/strong&gt; for timing operations, use this whenever my application leaves its domain of control &lt;em&gt;(e.g. when making database query or web requests)&lt;/em&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Custom stored procedures&lt;/strong&gt; for SQL Server, &lt;em&gt;see &lt;a href=&quot;#bonus&quot;&gt;bonus content&lt;/a&gt; below&lt;/em&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;&amp;lt;a href=&quot;https://grafana.com/oss/prometheus/?pg=get&amp;amp;plcmt=selfmanaged-box11-cta2&quot; target=&quot;_blank&quot;&amp;gt;Prometheus&amp;lt;/a&amp;gt;&lt;/h2&gt;
&lt;p&gt;Prometheus is a time series database and handles things that do not fit into structured logging such as performance counters. Like Loki, has its own query language called &amp;lt;a href=&quot;https://prometheus.io/docs/prometheus/latest/querying/basics/&quot; target=&quot;_blank&quot;&amp;gt;PromQL&amp;lt;/a&amp;gt;.&lt;/p&gt;
&lt;h4&gt;Performance counters&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;CPU and Memory Utilization&lt;/li&gt;
&lt;li&gt;Disk and Network Usage&lt;/li&gt;
&lt;li&gt;IIS and SQL specific counters like request throughput&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;These counters can be gathered from the servers directly using a tool called &amp;lt;a href=&quot;https://github.com/prometheus-community/windows_exporter&quot; target=&quot;_blank&quot;&amp;gt;windows_exporter&amp;lt;/a&amp;gt; that provides an HTTP endpoint that can be scraped by a Prometheus server.
:::warning
Please be mindful of your companies security practices as the out of the box the server is not secured. Configuration can be found &amp;lt;a href=&quot;https://github.com/prometheus/exporter-toolkit/blob/master/docs/web-configuration.md&quot; target=&quot;_blank&quot;&amp;gt;here&amp;lt;/a&amp;gt;.
:::&lt;/p&gt;
&lt;h1&gt;Logging Fundamentals&lt;/h1&gt;
&lt;p&gt;:::caution[Here be dragons...]&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Avoid logging PII -- Logs are public and can leak information&lt;/li&gt;
&lt;li&gt;Avoid excessive logging -- Logging can be great at first to build confidence in a process. If you find yourself logging every little step, &lt;code&gt;Debug&lt;/code&gt; or &lt;code&gt;Verbose&lt;/code&gt; should be used&lt;/li&gt;
&lt;li&gt;Avoid logging user correctable errors as &lt;code&gt;Error&lt;/code&gt;. &lt;code&gt;Debug&lt;/code&gt; or &lt;code&gt;Warning&lt;/code&gt; may be a more appropriate place for these types of messages&lt;/li&gt;
&lt;li&gt;Avoid punctuation when possible. Log messages are &lt;strong&gt;fragments, not sentences&lt;/strong&gt;
:::&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Log Key Events&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Major branching points in your code&lt;/li&gt;
&lt;li&gt;When errors or unexpected values are encountered&lt;/li&gt;
&lt;li&gt;Any IO or resource intensive operations&lt;/li&gt;
&lt;li&gt;Significant domain events&lt;/li&gt;
&lt;li&gt;Request failures and retires&lt;/li&gt;
&lt;li&gt;Beginning and end of time-consuming batch operations&lt;/li&gt;
&lt;/ul&gt;
&lt;h2&gt;Choose Appropriate Logging Level&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Be generous with your logging but strict with your logging levels. In almost all cases the level of your logs should be Debug&lt;/li&gt;
&lt;li&gt;Use Information for log events that would be needed in production to determine the running state or correctness of your application&lt;/li&gt;
&lt;li&gt;Use Warning or Error for unexpected events like exceptions&lt;/li&gt;
&lt;li&gt;The Error level should be reserved for events that you intend to act on. User correctable errors should never be logged at this level&lt;/li&gt;
&lt;/ul&gt;
&lt;h1&gt;Bonus -- Logging in SQL Server&lt;/h1&gt;
&lt;h2&gt;Configure&lt;/h2&gt;
&lt;p&gt;This is called at the start of your SQL session that you want to log and should only be called once per session.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[ConfigureFor](
	@Application VARCHAR(200),
	@Environment VARCHAR(11)
)
AS
BEGIN
	BEGIN TRY
		-- normalize and validate environment value
		SELECT @Environment = CASE
			WHEN @Environment IN (&apos;development&apos;, &apos;0&apos;) THEN &apos;Development&apos;
			WHEN @Environment IN (&apos;stage&apos;, &apos;2&apos;) THEN &apos;Stage&apos;
			WHEN @Environment IN (&apos;production&apos;, &apos;1&apos;) THEN &apos;Production&apos;
			ELSE NULL
		END

		IF @Environment IS NULL
		BEGIN
			RAISERROR(&apos;Invalid environment value. Should be one of [Development|Stage|Production]&apos;, 16, 1)
		END

		DECLARE @ReadOnly INT = 1
		EXECUTE sp_set_session_context &apos;Application&apos;, @Application, @ReadOnly
		EXECUTE sp_set_session_context &apos;MachineName&apos;, @@SERVERNAME, @ReadOnly
		EXECUTE sp_set_session_context &apos;Environment&apos;, @Environment, @ReadOnly

		EXECUTE sp_set_session_context &apos;__index__&apos;, NULL
	END TRY
	BEGIN CATCH
		THROW
	END CATCH
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Log Level Functions&lt;/h2&gt;
&lt;p&gt;The meat and potatoes of the implementation. It will gather all of the log properties off the stack and do the submission to the Loki server. There are convenience functions for each log level.&lt;/p&gt;
&lt;h3&gt;Implementation&lt;/h3&gt;
&lt;p&gt;:::important
This will need a way to make HTTP requests from SQL Server. This could be through &amp;lt;a href=&quot;https://learn.microsoft.com/en-us/sql/database-engine/configure-windows/ole-automation-procedures-server-configuration-option&quot; target=&quot;_blank&quot;&amp;gt;OLEAutomation&amp;lt;/a&amp;gt;, a dotnet SQLCLR assembly or SQL Server Language Extensions.
:::
:::note
This is typically not called directly favoring the convenience functions.
:::&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Impl](
	@Message NVARCHAR(4000),
	@Level NVARCHAR(50)
)
AS
BEGIN
	BEGIN TRY
		--DECLARE @Message NVARCHAR(4000) = &apos;DONKEY!&apos;
		-- cheap insurance to make sure that ConfigureFor has been called
		IF SESSION_CONTEXT(N&apos;Application&apos;) IS NULL
		BEGIN
			RETURN
		END

		-- get log time as unix epoch in nanos
		DECLARE @Time BIGINT = DATEDIFF_BIG(NANOSECOND, &apos;1970-01-01 00:00:00.0000000&apos;, SYSUTCDATETIME())

		-- add standard set of properties to be logged
		EXECUTE LogProperty_Add &apos;Message&apos;, @Message
		EXECUTE LogProperty_Add &apos;level&apos;, @Level

		-- send to loki
		-- json
		-- {
		-- 	&quot;streams&quot;: [
		-- 	{
		-- 		&quot;stream&quot;: {
		-- 			&quot;label&quot;: &quot;value&quot;
		-- 		},
		-- 		&quot;values&quot;: [
		-- 			[ &quot;unix epoch in nanoseconds&quot;, &quot;log line&quot; ],
		-- 			[ &quot;unix epoch in nanoseconds&quot;, &quot;log line&quot; ]
		-- 		]
		-- 	}
		-- 	]
		-- }
		-- 
		-- http://localhost:3100/loki/api/v1/push

		-- these are configured in the ConfigureFor stored procedure
		DECLARE @StreamLabels NVARCHAR(MAX) = (
			SELECT *
			FROM (
				SELECT *
				FROM(VALUES
					(&apos;Application&apos;, SESSION_CONTEXT(N&apos;Application&apos;)), 
					(&apos;MachineName&apos;, SESSION_CONTEXT(N&apos;MachineName&apos;)), 
					(&apos;Environment&apos;, SESSION_CONTEXT(N&apos;Environment&apos;))
				) stream_labels([key], [value])
			) a
			PIVOT (
				MAX([value]) FOR [key] IN ([Application], [MachineName], [Environment])
			) pvt
			FOR JSON AUTO, WITHOUT_ARRAY_WRAPPER/*, INCLUDE_NULL_VALUES*/
		)
		--SELECT @StreamLabels

		IF OBJECT_ID(&apos;tempdb..#TMP&apos;) IS NOT NULL
		BEGIN
			DROP TABLE #TMP
		END

		SELECT
			[key] = [value],
			[value] = SESSION_CONTEXT(CAST([value] AS NVARCHAR(100))),
			[is_json] = ISJSON(CAST(SESSION_CONTEXT(CAST([value] AS NVARCHAR(100))) AS NVARCHAR(MAX)))
		INTO #TMP
		FROM OPENJSON(CAST(SESSION_CONTEXT(N&apos;__index__&apos;) AS NVARCHAR(MAX)))

		-- generate select list to account for json column data.
		-- we have to use JSON_QUERY on json data so that it is treated as a json fragment when the overall select is serialized
		DECLARE @SelectList NVARCHAR(MAX) = (SELECT STUFF((
		-- stuff select begin
		SELECT &apos;,&apos; + IIF([is_json] = 1, QUOTENAME([key]) + &apos; = JSON_QUERY(CAST(&apos; + QUOTENAME([key]) + &apos; AS NVARCHAR(MAX)))&apos;, QUOTENAME([key]))
		FROM #TMP
		FOR XML PATH (&apos;&apos;)
		-- stuff select end
		), 1, 1, &apos;&apos;)
		)
		--SELECT @SelectList

		-- generate pivot list
		DECLARE @PvtList NVARCHAR(MAX) = (SELECT STUFF((
		-- stuff select begin
		SELECT &apos;,&apos; + QUOTENAME([key]) FROM #TMP
		FOR XML PATH (&apos;&apos;)
		-- stuff select end
		), 1, 1, &apos;&apos;)
		)
		--SELECT @PvtList

		-- generate message span
		DECLARE @Sql NVARCHAR(MAX) = &apos;&apos;
		SELECT @Sql = &apos;
		DECLARE @Json NVARCHAR(MAX) = (
			SELECT {1}
			FROM (
				SELECT [key], [value]
				FROM #TMP
			) a
			PIVOT (
				MAX([value]) FOR [key] IN ({0})
			) pvt
			FOR JSON AUTO, WITHOUT_ARRAY_WRAPPER/*, INCLUDE_NULL_VALUES*/
		)
		SELECT @Json&apos;
		SELECT @Sql = REPLACE(@Sql, &apos;{0}&apos;, @PvtList)
		SELECT @Sql = REPLACE(@Sql, &apos;{1}&apos;, @SelectList)

		DECLARE @Json TABLE ([Json] NVARCHAR(MAX))
		INSERT INTO @Json
		EXECUTE(@Sql)

		-- generate full loki request
		DECLARE @LokiRequest NVARCHAR(MAX) = (
			SELECT
				[stream] = JSON_QUERY(@StreamLabels),
				[values] = JSON_QUERY(&apos;[[&apos; + QUOTENAME(CAST(@Time AS VARCHAR(20)), &apos;&quot;&apos;) + &apos;,&quot;&apos; + STRING_ESCAPE([Json], &apos;json&apos;) + &apos;&quot;]]&apos;)
			FROM @Json
			FOR JSON AUTO, ROOT(&apos;streams&apos;)
		)
		--SELECT @LokiRequest
		--PRINT @LokiRequest

		-- reset exception
		EXECUTE LogProperty_Add &apos;Exception&apos;, NULL

		-- loose the request
		DECLARE @HeadersJson NVARCHAR(MAX) = (
			SELECT *
			FROM (
				SELECT 
					[content-type] = &apos;application/json&apos;,
					[content-length] = CAST(LEN(@LokiRequest) AS VARCHAR(20))
			) a
			FOR JSON AUTO
		)
		--SELECT @HeadersJson

		EXECUTE HttpPost &apos;http://localhost:3100/loki/api/v1/push&apos;, @LokiRequest, NULL, NULL, @HeadersJson
	END TRY
	BEGIN CATCH
		-- swallow
	END CATCH
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Fatal&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Fatal](
	@Message NVARCHAR(4000),
	@CallingProc SYSNAME = NULL
)
AS
BEGIN
	IF @CallingProc IS NOT NULL
	BEGIN
		EXECUTE LogProperty_AddSourceContext @CallingProc
	END

	-- extra error details
	IF ERROR_MESSAGE() IS NOT NULL
	BEGIN
		DECLARE @Exception NVARCHAR(4000)
		SELECT @Exception = (
			SELECT *
			FROM (
				SELECT 
					[ErrorNumber] = ERROR_NUMBER(),
					[ErrorSeverity] = ERROR_SEVERITY(),
					[ErrorState] = ERROR_STATE(),
					[ErrorProcedure] = ERROR_PROCEDURE(),
					[ErrorLine] = ERROR_LINE(),
					[ErrorMessage] = ERROR_MESSAGE()
			) exception
			FOR JSON AUTO, WITHOUT_ARRAY_WRAPPER
		)

		EXECUTE LogProperty_Add &apos;Exception&apos;, @Exception
	END

	EXECUTE LogTo_Impl @Message, &apos;critical&apos;
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Error&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Error](
	@Message NVARCHAR(4000),
	@CallingProc SYSNAME = NULL
)
AS
BEGIN
	IF @CallingProc IS NOT NULL
	BEGIN
		EXECUTE LogProperty_AddSourceContext @CallingProc
	END

	-- extra error details
	IF ERROR_MESSAGE() IS NOT NULL
	BEGIN
		DECLARE @Exception NVARCHAR(4000)
		SELECT @Exception = (
			SELECT *
			FROM (
				SELECT 
					[ErrorNumber] = ERROR_NUMBER(),
					[ErrorSeverity] = ERROR_SEVERITY(),
					[ErrorState] = ERROR_STATE(),
					[ErrorProcedure] = ERROR_PROCEDURE(),
					[ErrorLine] = ERROR_LINE(),
					[ErrorMessage] = ERROR_MESSAGE()
			) exception
			FOR JSON AUTO, WITHOUT_ARRAY_WRAPPER
		)

		EXECUTE LogProperty_Add &apos;Exception&apos;, @Exception
	END

	EXECUTE LogTo_Impl @Message, &apos;error&apos;
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Debug&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Debug](
	@Message NVARCHAR(4000),
	@CallingProc SYSNAME = NULL
)
AS
BEGIN
	IF @CallingProc IS NOT NULL
	BEGIN
		EXECUTE LogProperty_AddSourceContext @CallingProc
	END

	EXECUTE LogTo_Impl @Message, &apos;debug&apos;
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Warning&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Warning](
	@Message NVARCHAR(4000),
	@CallingProc SYSNAME = NULL
)
AS
BEGIN
	IF @CallingProc IS NOT NULL
	BEGIN
		EXECUTE LogProperty_AddSourceContext @CallingProc
	END

	EXECUTE LogTo_Impl @Message, &apos;warn&apos;
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Information&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Information](
	@Message NVARCHAR(4000),
	@CallingProc SYSNAME = NULL
)
AS
BEGIN
	IF @CallingProc IS NOT NULL
	BEGIN
		EXECUTE LogProperty_AddSourceContext @CallingProc
	END

	EXECUTE LogTo_Impl @Message, &apos;info&apos;
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;Verbose&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;CREATE PROCEDURE [dbo].[LogTo_Verbose](
	@Message NVARCHAR(4000),
	@CallingProc SYSNAME = NULL
)
AS
BEGIN
	IF @CallingProc IS NOT NULL
	BEGIN
		EXECUTE LogProperty_AddSourceContext @CallingProc
	END

	EXECUTE LogTo_Impl @Message, &apos;trace&apos;
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Log Property Stack&lt;/h2&gt;
&lt;p&gt;This is how you add additional context to your logs with some convenience functions below. They are just key value pairs that will get added to the log stream and can be parsed out using the Loki logQL within Grafana.&lt;/p&gt;
&lt;h3&gt;Implementation&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;ALTER PROCEDURE [dbo].[LogProperty_Add](
	@Name NVARCHAR(100),
	@Value NVARCHAR(4000)
)
AS
BEGIN
	BEGIN TRY
		--EXECUTE sp_set_session_context &apos;__index__&apos;, &apos;[&quot;ApplicationInstance&quot;, &quot;CorrelationId&quot;]&apos;
		--DECLARE @Name VARCHAR(100) = &apos;InstanceId&apos;

		-- you cannot query values from the SESSION_CONTEXT without explicitly knowing the key
		-- for this reason, we house a list of keys in a __index__ session value so we know what
		-- to pull when we generate the actual log
		-- add new key to property bag
		DECLARE @IndexKeys NVARCHAR(4000)
		SELECT @IndexKeys = &apos;[&apos; + ISNULL(STUFF(
-- stuff select begin
(SELECT &apos;,&quot;&apos; + [Value] + &apos;&quot;&apos;
FROM (
	SELECT [value]
	FROM OPENJSON(CAST(SESSION_CONTEXT(N&apos;__index__&apos;) AS NVARCHAR(MAX)))
	UNION
	SELECT @Name
) a([value])
FOR XML PATH (&apos;&apos;)),
-- stuff select end
		1, 1, &apos;&apos;), &apos;&apos;) + &apos;]&apos;
		
		-- persist new property bag
		EXECUTE sp_set_session_context &apos;__index__&apos;, @IndexKeys
		--SELECT SESSION_CONTEXT(N&apos;__index__&apos;)

		-- persist log property
		EXECUTE sp_set_session_context @Name, @Value
	END TRY
	BEGIN CATCH
		-- swallow
	END CATCH
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;CorrelationId&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;ALTER PROCEDURE [dbo].[LogProperty_CorrelationId](
	@Value NVARCHAR(4000)
)
AS
BEGIN
	EXECUTE LogProperty_Add &apos;CorrelationId&apos;, @Value
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;SourceContext&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;ALTER PROCEDURE [dbo].[LogProperty_AddSourceContext](
	@Value NVARCHAR(4000)
)
AS
BEGIN
	EXECUTE LogProperty_Add &apos;SourceContext&apos;, @Value
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h3&gt;InstanceId&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;ALTER PROCEDURE [dbo].[LogProperty_InstanceId](
	@Value NVARCHAR(4000)
)
AS
BEGIN
	EXECUTE LogProperty_Add &apos;InstanceId&apos;, @Value
END
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>SQL Server Quirky Update</title><link>https://www.kichka.dev/posts/sql-quirky-update/</link><guid isPermaLink="true">https://www.kichka.dev/posts/sql-quirky-update/</guid><description>Controversial running total calculations in SQL Server using the &quot;Quirky Update&quot;</description><pubDate>Sun, 28 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;While I was developing my trading algo, I wanted to calculate different technical indicators, most of which depend on previous data points. In order to do that in SQL Server you have a couple options, and this is one of them.&lt;/p&gt;
&lt;p&gt;There are two key points that make this &quot;quirky update&quot; work.&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;You &lt;strong&gt;MUST&lt;/strong&gt; have a clustered index on the table that represents the &lt;strong&gt;expected sort order&lt;/strong&gt; of the data. In this case is was the tick data ascending.&lt;/li&gt;
&lt;li&gt;You &lt;strong&gt;MUST&lt;/strong&gt; use &lt;code&gt;OPTION (MAXDOP 1)&lt;/code&gt; option on the query so SQL Server doesn&apos;t try to parallelize the update.&lt;/li&gt;
&lt;/ol&gt;
&lt;h1&gt;Setup&lt;/h1&gt;
&lt;p&gt;Below we create a staging table to house the technical indicators, the primary key is the clustered index, and tick data is inserted into the table in sorted order. You could also have the clustered index on the &lt;code&gt;[Time]&lt;/code&gt; column as well.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;--DROP TABLE [stage].[Meta]
CREATE TABLE [stage].[Meta] (
	[RowIdx] BIGINT IDENTITY PRIMARY KEY,
	[Id] UNIQUEIDENTIFIER,
	[InstrumentId] INT,
	[Pip] NUMERIC(10, 5),
	[Time] DATETIME2,
	[Open] NUMERIC(10, 5),
	[High] NUMERIC(10, 5),
	[Low] NUMERIC(10, 5),
	[Close] NUMERIC(10, 5),
	[Volume] INT
);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;In the following examples take note of the usage of the variables that retain the previous calculation result and how they get used when updating the calculation for the next row. This essentially causes the same behavior you would get if you had used a SQL &lt;code&gt;CURSOR&lt;/code&gt;.&lt;/p&gt;
&lt;h1&gt;Example -- Heikin Ashi Candlestick&lt;/h1&gt;
&lt;pre&gt;&lt;code&gt;/* indicator columns
ALTER TABLE [stage].[Meta]
ADD	[HaOpen] NUMERIC(10, 5),
    [HaHigh] NUMERIC(10, 5),
    [HaLow] NUMERIC(10, 5),
    [HaClose] NUMERIC(10, 5)
--*/

-- calculate heikin ashi candles
-- NOTE: variable ordering matters here
---------------------------------------------------------------------------------------------------
DECLARE
    @HaOpen NUMERIC(10, 5) = 0,
    @HaClose NUMERIC(10, 5) = 0,
    @PrevHaOpen NUMERIC(10, 5) = 0,
    @PrevHaClose NUMERIC(10, 5) = 0

-- quirky update --
UPDATE [stage].[Meta]
SET @HaClose = ([Open] + [High] + [Low] + [Close]) / 4.0,
    [HaClose] = @HaClose,

    @HaOpen = CASE WHEN [RowIdx] = 1 THEN ([Open] + [Close]) / 2.0 ELSE (@PrevHaOpen + @PrevHaClose) / 2.0 END,
    [HaOpen] = @HaOpen,

    [HaHigh] = (SELECT MAX([val]) FROM ( VALUES ([High]), ([HaOpen]), ([HaClose]) ) T([val])),
    [HaLow] = (SELECT MIN([val]) FROM ( VALUES ([Low]), ([HaOpen]), ([HaClose]) ) T([val])),

    @PrevHaClose = @HaClose,
    @PrevHaOpen = @HaOpen
OPTION (MAXDOP 1);
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Example -- Exponential Moving Average&lt;/h1&gt;
&lt;pre&gt;&lt;code&gt;/* indicator columns
ALTER TABLE [stage].[Meta]
ADD [EMA012] NUMERIC(10, 5),
    [EMA026] NUMERIC(10, 5)
--*/

-- calculate ema&apos;s
-- NOTE: depends on sma being calculated first
---------------------------------------------------------------------------------------------------
-- quirky update --
DECLARE
    @EMA012 NUMERIC(10, 5) = 0,
    @EMA026 NUMERIC(10, 5) = 0

UPDATE [stage].[Meta]
SET @EMA012 = CASE WHEN [RowIdx] = 1 THEN [SMA012] ELSE ([Close] - @EMA012) * (2 / ((12 + 1) * 1.0)) + @EMA012 END,
    [EMA012] = @EMA012,

    @EMA026 = CASE WHEN [RowIdx] = 1 THEN [SMA026] ELSE ([Close] - @EMA026) * (2 / ((26 + 1) * 1.0)) + @EMA026 END,
    [EMA026] = @EMA026
OPTION (MAXDOP 1);
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Summary&lt;/h1&gt;
&lt;p&gt;I first came across this when looking for a better way to perform the update without having all of the ceremony that comes with the &lt;code&gt;CURSOR&lt;/code&gt;. You can find the original article below and their 10 rules on using this technique.&lt;/p&gt;
&lt;p&gt;&amp;lt;a href=&quot;https://www.sqlservercentral.com/articles/solving-the-running-total-and-ordinal-rank-problems-rewritten&quot; target=&quot;_blank&quot;&amp;gt;The Rules &amp;amp; Inspiration&amp;lt;/a&amp;gt;&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;CLUSTERED INDEX MUST BE PRESENT IN THE CORRECT ORDER&lt;/li&gt;
&lt;li&gt;PARALLELISM MUST BE PREVENTED: MUST include OPTION (MAXDOP 1)&lt;/li&gt;
&lt;li&gt;DON&apos;T WORK AGAINST PARTITIONED STRUCTURES&lt;/li&gt;
&lt;li&gt;USE THE TABLOCKX HINT&lt;/li&gt;
&lt;li&gt;DO NOT USE JOINS&lt;/li&gt;
&lt;li&gt;YOU MUST HAVE AN &quot;ANCHOR&quot; COLUMN&lt;/li&gt;
&lt;li&gt;DO NOT USE ORDER BY&lt;/li&gt;
&lt;li&gt;DO NOT USE INDEX HINTS TO TRY TO FORCE ORDER&lt;/li&gt;
&lt;li&gt;GET IT RIGHT&lt;/li&gt;
&lt;li&gt;TEST!&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>SQL Voodoo Black Magic</title><link>https://www.kichka.dev/posts/sql-voodoo-blackmagic/</link><guid isPermaLink="true">https://www.kichka.dev/posts/sql-voodoo-blackmagic/</guid><description>SQL snips for querying sysobjects table and using the results to dynamically generate SQL query</description><pubDate>Sat, 27 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;Sometimes you want to be able to dynamically construct table definitions to perform different actions, such as creating a copy of a table on a different database, compare table schema, sync table data where the tables are not guarenteed to have the same schema, generate update, merge, select and insert statements and not have to keep adjusting the code as the table schema drifts.&lt;/p&gt;
&lt;p&gt;Below we will go through a few of these scenarios, but first lets define the &quot;black magic&quot; that the rest of the examples will build upon.&lt;/p&gt;
&lt;h1&gt;Black Magic&lt;/h1&gt;
&lt;p&gt;These are the exact same query but produce a &lt;code&gt;#Source&lt;/code&gt; and &lt;code&gt;#Destination&lt;/code&gt; temp tables that the examples will use.&lt;/p&gt;
&lt;h2&gt;Select Source Schema&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;DECLARE
	@TableName SYSNAME = &apos;MyTable&apos;,
	@TableSchema SYSNAME = &apos;dbo&apos;

IF OBJECT_ID(&apos;tempdb..#Source&apos;) IS NOT NULL
	DROP TABLE #Source

SELECT
	[ColumnId] = sc.[column_id],
	[ColumnName] = sc.[name] COLLATE DATABASE_DEFAULT,
	-- adjust timestamp type to varbinary
	[Type] = CASE WHEN st.[user_type_id] = 189 THEN &apos;varbinary&apos; ELSE st.[name] END COLLATE DATABASE_DEFAULT,
	-- adjust nvarchar length to half what is reported since it takes 2 bytes to store unicode, and adjust timestamp to the translated varbinary length
	[Length] = CASE WHEN sc.[max_length] = -1 THEN -1 WHEN st.[user_type_id] = 231 THEN sc.[max_length] / 2 WHEN st.[user_type_id] = 189 THEN 85 ELSE sc.[max_length] END,
	[Precision] = sc.[precision], 
	[Scale] = sc.[scale],
	[IsNullable] = sc.[is_nullable]
INTO #Source
FROM [sys].[objects] so
JOIN [sys].[columns] sc
ON so.[object_id] = sc.[object_id]
JOIN [sys].[types] st
ON sc.[user_type_id] = st.[user_type_id]
JOIN [sys].[schemas] ss
ON so.[schema_id] = ss.[schema_id]
WHERE so.[name] = @TableName AND ss.[name] = @TableSchema
&lt;/code&gt;&lt;/pre&gt;
&lt;h2&gt;Select Destination Schema&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;DECLARE
	@TableName SYSNAME = &apos;MyTable&apos;,
	@TableSchema SYSNAME = &apos;dbo&apos;

IF OBJECT_ID(&apos;tempdb..#Destination&apos;) IS NOT NULL
	DROP TABLE #Destination

SELECT
	[ColumnId] = sc.[column_id],
	[ColumnName] = sc.[name] COLLATE DATABASE_DEFAULT,
	-- adjust timestamp type to varbinary
	[Type] = CASE WHEN st.[user_type_id] = 189 THEN &apos;varbinary&apos; ELSE st.[name] END COLLATE DATABASE_DEFAULT,
	-- adjust nvarchar length to half what is reported since it takes 2 bytes to store unicode, and adjust timestamp to the translated varbinary length
	[Length] = CASE WHEN sc.[max_length] = -1 THEN -1 WHEN st.[user_type_id] = 231 THEN sc.[max_length] / 2 WHEN st.[user_type_id] = 189 THEN 85 ELSE sc.[max_length] END,
	[Precision] = sc.[precision], 
	[Scale] = sc.[scale],
	[IsNullable] = sc.[is_nullable]
INTO #Destination
FROM [sys].[objects] so
JOIN [sys].[columns] sc
ON so.[object_id] = sc.[object_id]
JOIN [sys].[types] st
ON sc.[user_type_id] = st.[user_type_id]
JOIN [sys].[schemas] ss
ON so.[schema_id] = ss.[schema_id]
WHERE so.[name] = @TableName AND ss.[name] = @TableSchema
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Example -- Create Table Copy&lt;/h1&gt;
&lt;p&gt;Create a copy of the table using the source schema information possibly injecting extra columns in the copy.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;DECLARE
	@TableName SYSNAME = &apos;MyTable&apos;,
	@TableSchema SYSNAME = &apos;dbo&apos;,
	@IsDebug BIT = 0

DECLARE
	@DataType NVARCHAR(200),
	@Length INT,
	@Precision INT,
	@Scale INT,
	@IsNullable INT,
	@ColumnName NVARCHAR(200),
	@Statement NVARCHAR(MAX) = &apos;&apos;

DECLARE x CURSOR FAST_FORWARD FOR
	SELECT [ColumnName], [Type], [Length], [Precision], [Scale], [IsNullable]
	FROM #Source
	ORDER BY [ColumnId]

OPEN x
FETCH NEXT FROM x INTO @ColumnName, @DataType, @Length, @Precision, @Scale, @IsNullable
WHILE @@FETCH_STATUS = 0
BEGIN
	DECLARE @NullClause NVARCHAR(10) = &apos; NULL&apos;
	IF @IsNullable = 0
	BEGIN
		SELECT @NullClause = &apos; NOT NULL&apos;
	END

	IF @DataType = &apos;timestamp&apos;
	BEGIN
		SELECT @DataType = &apos;varbinary&apos;
		SELECT @Length = 85
	END

	IF @DataType IN(&apos;char&apos;,&apos;varchar&apos;,&apos;binary&apos;,&apos;varbinary&apos;,&apos;nvarchar&apos;,&apos;nchar&apos;)
	BEGIN
		SELECT @Statement = @Statement + &apos;[&apos; + @ColumnName + &apos;] &apos; + @DataType + &apos;(&apos; + ISNULL(NULLIF(CONVERT(NVARCHAR,@Length), &apos;-1&apos;), &apos;MAX&apos;) + &apos;)&apos; + @NullClause + &apos;,&apos;
	END
	ELSE IF @DataType IN(&apos;decimal&apos;,&apos;numeric&apos;)
	BEGIN
		SELECT @Statement = @Statement + &apos;[&apos; + @ColumnName + &apos;] &apos; + @DataType + &apos;(&apos; + CONVERT(NVARCHAR,@Precision) + &apos;,&apos; + CONVERT(NVARCHAR,@Scale) + &apos;)&apos;  + @NullClause + &apos;,&apos;
	END
	ELSE
	BEGIN
		SELECT @Statement = @Statement + &apos;[&apos; + @ColumnName + &apos;] &apos; + @DataType + @NullClause + &apos;,&apos;
	END
FETCH NEXT FROM x INTO @ColumnName, @DataType, @Length, @Precision, @Scale, @IsNullable
END
CLOSE x
DEALLOCATE x

SELECT @Statement = SUBSTRING(@Statement, 1, LEN(@Statement) - 1)

DECLARE @Sql NVARCHAR(MAX) = &apos;&apos;

SELECT @Sql = &apos;
CREATE TABLE [{0}].[{1}](
{2}
)&apos;

SELECT @Sql = REPLACE(@Sql, &apos;{0}&apos;, @TableSchema)
SELECT @Sql = REPLACE(@Sql, &apos;{1}&apos;, @TableName)
SELECT @Sql = REPLACE(@Sql, &apos;{2}&apos;, @Statement)

IF @IsDebug = 1
	PRINT (@Sql)
IF @IsDebug = 0
	EXEC (@Sql)
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Example -- Schema Diff&lt;/h1&gt;
&lt;p&gt;Below are a few ways you can use this information to find;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;columns that have changed&lt;/li&gt;
&lt;li&gt;columns that are missing from the destination&lt;/li&gt;
&lt;li&gt;columns that have been removed from the source and not the destination&lt;/li&gt;
&lt;li&gt;columns that both tables have in common&lt;/li&gt;
&lt;/ul&gt;
&lt;pre&gt;&lt;code&gt;IF OBJECT_ID(&apos;tempdb..#CHANGED_COLUMNS&apos;) IS NOT NULL
	DROP TABLE #CHANGED_COLUMNS

SELECT
    [ColumnName],
    [Type],
    [Length],
    [Precision], 
    [Scale]
INTO #CHANGED_COLUMNS
FROM #Source
EXCEPT
SELECT
    [ColumnName],
    [Type],
    [Length],
    [Precision], 
    [Scale]
FROM #Destination


IF OBJECT_ID(&apos;tempdb..#MISSING_COLUMNS&apos;) IS NOT NULL
	DROP TABLE #MISSING_COLUMNS

SELECT
    [ColumnName]
INTO #MISSING_COLUMNS
FROM #Source
EXCEPT
SELECT
    [ColumnName]
FROM #Destination

IF OBJECT_ID(&apos;tempdb..#DELETED_COLUMNS&apos;) IS NOT NULL
	DROP TABLE #DELETED_COLUMNS

SELECT
    [ColumnName]
INTO #DELETED_COLUMNS
FROM #Destination
EXCEPT
SELECT
    [ColumnName]
FROM #SOURCE

IF OBJECT_ID(&apos;tempdb..#SHARED_COLUMNS&apos;) IS NOT NULL
	DROP TABLE #SHARED_COLUMNS

SELECT
    [ColumnName]
INTO #SHARED_COLUMNS
FROM #Source
INTERSECT
SELECT
    [ColumnName]
FROM #Destination
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Example -- Sync Destination Schema with Source&lt;/h1&gt;
&lt;p&gt;Here we will cursor over the columns that are missing and either create a series of &lt;code&gt;ALTER TABLE&lt;/code&gt; statements to bring the destination table in sync with the source.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;DECLARE
	@TableName SYSNAME = &apos;MyTable&apos;,
	@TableSchema SYSNAME = &apos;dbo&apos;,
	@IsDebug BIT = 0

IF OBJECT_ID(&apos;tempdb..#CHANGED_COLUMNS&apos;) IS NOT NULL
	DROP TABLE #CHANGED_COLUMNS

SELECT
    [ColumnName],
    [Type],
    [Length],
    [Precision], 
    [Scale]
INTO #CHANGED_COLUMNS
FROM #Source
EXCEPT
SELECT
    [ColumnName],
    [Type],
    [Length],
    [Precision], 
    [Scale]
FROM #Destination

DECLARE
	@Statement NVARCHAR(MAX) = &apos;&apos;,
	@AlterColumnStatement NVARCHAR(MAX) = &apos;&apos;,
	@ColumnName NVARCHAR(200),
	@Sql NVARCHAR(MAX) = &apos;&apos;

IF EXISTS (SELECT 1 FROM #CHANGED_COLUMNS)
BEGIN
	DECLARE 
		@DataType NVARCHAR(200),
		@Length INT,
		@Precision INT,
		@Scale INT

	DECLARE
		@HasExistingColumns INT = 0,
		@HasNewColumns INT = 0

	DECLARE x CURSOR FOR
		SELECT [ColumnName], [Type], [Length], [Precision], [Scale]
		FROM #COLUMNS
		ORDER BY 1

	OPEN x
	FETCH NEXT FROM x INTO @ColumnName, @DataType, @Length, @Precision, @Scale
	WHILE @@FETCH_STATUS = 0
	BEGIN
		DECLARE @Part NVARCHAR(MAX)
		DECLARE @AlterColumnStub NVARCHAR(MAX) = &apos;
ALTER TABLE [{2}].[{0}]
ALTER COLUMN {1}&apos;

		DECLARE @ColumnExists INT = 0
		IF EXISTS(SELECT 1 FROM #Destination WHERE [ColumnName] = @ColumnName)
		BEGIN
			SELECT @ColumnExists = 1
			SELECT @HasExistingColumns = 1
		END
		ELSE
		BEGIN
			SELECT @HasNewColumns = 1
		END

		IF @DataType = &apos;timestamp&apos;
		BEGIN
			SELECT @DataType = &apos;varbinary&apos;
			SELECT @Length = 85
		END

		IF @DataType IN(&apos;char&apos;,&apos;varchar&apos;,&apos;binary&apos;,&apos;varbinary&apos;,&apos;nvarchar&apos;,&apos;nchar&apos;)
		BEGIN
			SELECT @Part = &apos;[&apos; + @ColumnName + &apos;] &apos; + @DataType + &apos;(&apos; + ISNULL(NULLIF(CONVERT(NVARCHAR,@Length), &apos;-1&apos;), &apos;MAX&apos;) + &apos;)&apos;

			IF(@ColumnExists = 1)
			BEGIN
				SELECT @AlterColumnStatement = @AlterColumnStatement + REPLACE(@AlterColumnStub, &apos;{1}&apos;, @Part)
			END
			ELSE
			BEGIN
				SELECT @Statement = @Statement + @Part + &apos;,&apos;
			END
		END
		ELSE IF @DataType IN(&apos;decimal&apos;,&apos;numeric&apos;)
		BEGIN
			SELECT @Part = &apos;[&apos; + @ColumnName + &apos;] &apos; + @DataType + &apos;(&apos; + CONVERT(NVARCHAR,@Precision) + &apos;,&apos; + CONVERT(NVARCHAR,@Scale) + &apos;)&apos;

			IF(@ColumnExists = 1)
			BEGIN
				SELECT @AlterColumnStatement = @AlterColumnStatement + REPLACE(@AlterColumnStub, &apos;{1}&apos;, @Part)
			END
			ELSE
			BEGIN
				SELECT @Statement = @Statement + @Part + &apos;,&apos;
			END
		END
		ELSE
		BEGIN
			SELECT @Part = &apos;[&apos; + @ColumnName + &apos;] &apos; + @DataType

			IF(@ColumnExists = 1)
			BEGIN
				SELECT @AlterColumnStatement = @AlterColumnStatement + REPLACE(@AlterColumnStub, &apos;{1}&apos;, @Part)
			END
			ELSE
			BEGIN
				SELECT @Statement = @Statement + @Part + &apos;,&apos;
			END
		END
	FETCH NEXT FROM x INTO @ColumnName, @DataType, @Length, @Precision, @Scale
	END
	CLOSE x
	DEALLOCATE x

	IF (LEN(@Statement) &amp;gt; 1)
	BEGIN
		SELECT @Statement = SUBSTRING(@Statement, 1, LEN(@Statement) - 1)
	END

	-- determine if is alter or create statement
	IF OBJECT_ID(@EntityName) IS NOT NULL
	BEGIN
		IF (@HasExistingColumns = 1)
		BEGIN
			SELECT @Sql += @AlterColumnStatement
		END
		
		IF (@HasNewColumns = 1)
		BEGIN
			SELECT @Sql += &apos;
ALTER TABLE [{2}].[{0}]
ADD {1}&apos;
		END
	END

	SELECT @Sql = REPLACE(@Sql, &apos;{0}&apos;, @TableName)
	SELECT @Sql = REPLACE(@Sql, &apos;{1}&apos;, @Statement)
	SELECT @Sql = REPLACE(@Sql, &apos;{2}&apos;, @TableSchema)

    IF @IsDebug = 1
        PRINT (@Sql)
    IF @IsDebug = 0
        EXEC (@Sql)
END
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Example -- Generate Statement Layouts&lt;/h1&gt;
&lt;p&gt;Here we generate layouts that can be used in &lt;code&gt;SELECT&lt;/code&gt;, &lt;code&gt;INSERT&lt;/code&gt; and &lt;code&gt;UPDATE&lt;/code&gt; statements assuming the source table is aliased as &lt;code&gt;[source]&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;IF OBJECT_ID(&apos;tempdb..#SHARED_COLUMNS&apos;) IS NOT NULL
	DROP TABLE #SHARED_COLUMNS

SELECT
    [ColumnName]
INTO #SHARED_COLUMNS
FROM #Source
INTERSECT
SELECT
    [ColumnName]
FROM #Destination

-- generate layout --
DECLARE
    @ColumnFormat NVARCHAR(MAX) = &apos;&apos;,
    @UpdateFormat NVARCHAR(MAX) = &apos;&apos;,
    @ValuesFormat NVARCHAR(MAX) = &apos;&apos;

SELECT
    @ColumnFormat = STUFF((SELECT &apos;, [&apos; + [ColumnName] + &apos;]&apos; FROM #SHARED_COLUMNS ORDER BY [ColumnName] FOR XML PATH(&apos;&apos;)), 1, 2, &apos;&apos;),
    @UpdateFormat = STUFF((SELECT &apos;, [&apos; + [ColumnName] + &apos;] = [source].[&apos; + [ColumnName] + &apos;]&apos; FROM #SHARED_COLUMNS ORDER BY [ColumnName] FOR XML PATH(&apos;&apos;)), 1, 2, &apos;&apos;),
    @ValuesFormat = STUFF((SELECT &apos;, [source].[&apos; + [ColumnName] + &apos;]&apos; FROM #SHARED_COLUMNS ORDER BY [ColumnName] FOR XML PATH(&apos;&apos;)), 1, 2, &apos;&apos;)

PRINT @ColumnFormat
PRINT @UpdateFormat
PRINT @ValuesFormat
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>SQL Server Temporal Tables</title><link>https://www.kichka.dev/posts/sql-temporal-tables/</link><guid isPermaLink="true">https://www.kichka.dev/posts/sql-temporal-tables/</guid><description>SQL snips for querying and enabling temporal tables</description><pubDate>Tue, 23 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;System Version Query -- How To&lt;/h1&gt;
&lt;pre&gt;&lt;code&gt;SELECT * 
FROM dbo.MyTable

--( at time zone does not work on literals, use var to work around this limitation )--
DECLARE @AsOf DATETIME2 = &apos;4/8/2019 9:43AM&apos;
SELECT @AsOf = @AsOf AT TIME ZONE &apos;Eastern Standard Time&apos; AT TIME ZONE &apos;UTC&apos;

SELECT *
FROM dbo.MyTable
FOR SYSTEM_TIME AS OF @AsOf --( includes changes where the record was valid during the given time )--
--FOR SYSTEM_TIME ALL --( includes all changes )--
--FOR SYSTEM_TIME FROM @StartDate TO @EndDate --( exclusive of start and end dates )--
--FOR SYSTEM_TIME BETWEEN @StartDate AND @EndDate --( inclusive of start and end dates )--

--( query directly from the history table )--
SELECT *
FROM dbo.MyTableHistory
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Enable System Versioning&lt;/h1&gt;
&lt;p&gt;This snippet will add the required columns &lt;code&gt;ValidFrom&lt;/code&gt; and &lt;code&gt;ValidTo&lt;/code&gt; and enable system versioning with a history table of &lt;code&gt;{Schema}.{TableName}History&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;DECLARE
	@TableName SYSNAME = &apos;MyTable&apos;,
	@TableSchema SYSNAME = &apos;dbo&apos;,
	@IsDebug BIT = 0

DECLARE @Sql NVARCHAR(MAX) = &apos;&apos;

--( add sys version columns and enable )--
SELECT @Sql = &apos;
ALTER TABLE [{0}].[{1}]
ADD [ValidFrom] DATETIME2 GENERATED ALWAYS AS ROW START HIDDEN NOT NULL CONSTRAINT [DF[{0}]].[{1}]].[ValidFrom]]]  DEFAULT (sysutcdatetime()),
[ValidTo] DATETIME2 GENERATED ALWAYS AS ROW END HIDDEN NOT NULL CONSTRAINT [DF[{0}]].[{1}]].[ValidTo]]] DEFAULT (CONVERT([datetime2](0),&apos;&apos;9999-12-31 23:59:59&apos;&apos;)),
PERIOD FOR SYSTEM_TIME([ValidFrom], [ValidTo])

ALTER TABLE [{0}].[{1}]
SET (SYSTEM_VERSIONING = ON (HISTORY_TABLE = [{0}].[{1}History]));
&apos;
SELECT @Sql = REPLACE(@Sql, &apos;{0}&apos;, @TableSchema)
SELECT @Sql = REPLACE(@Sql, &apos;{1}&apos;, @TableName)

IF @IsDebug = 1
	PRINT (@Sql)
IF @IsDebug = 0
	EXEC (@Sql)
&lt;/code&gt;&lt;/pre&gt;
&lt;h1&gt;Disable System Versioning&lt;/h1&gt;
&lt;p&gt;This will disable system versioning on the given table.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;DECLARE
	@TableName SYSNAME = &apos;MyTable&apos;,
	@TableSchema SYSNAME = &apos;dbo&apos;,
	@IsDebug BIT = 0

DECLARE @Sql NVARCHAR(MAX) = &apos;&apos;

SELECT @Sql = &apos;
ALTER TABLE [{0}].[{1}]
SET (SYSTEM_VERSIONING = OFF)
&apos;
SELECT @Sql = REPLACE(@Sql, &apos;{0}&apos;, @TableSchema)
SELECT @Sql = REPLACE(@Sql, &apos;{1}&apos;, @TableName)

IF @IsDebug = 1
	PRINT (@Sql)
IF @IsDebug = 0
	EXEC (@Sql)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>SQL Server See What&apos;s Running</title><link>https://www.kichka.dev/posts/sql-see-whats-running/</link><guid isPermaLink="true">https://www.kichka.dev/posts/sql-see-whats-running/</guid><description>This is a collection of SQL snippets for tracking down what is running and what might be misbehaving</description><pubDate>Mon, 22 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;p&gt;Below are a collection of SQL query that I use to diagnose and trace what is running on the server.&lt;/p&gt;
&lt;p&gt;This is my goto snippet to quickly identify what is running and the current state of the server. It will provide you cpu and disk read time as well as the query plans which can be used to identify problem children.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;DROP TABLE #PERFSNAP

SELECT
    r.cpu_time,
    r.logical_reads,
    r.session_id 
INTO #TEMP
FROM sys.dm_exec_sessions s 
JOIN sys.dm_exec_requests r 
ON s.session_id = r.session_id
--AND s.last_request_start_time = r.start_time
WHERE is_user_process = 1 
 
WAITFOR DELAY &apos;00:00:01&apos;

SELECT 
    [text] = SUBSTRING(
        h.text, 
        (r.statement_start_offset / 2) + 1,
        ((  CASE r.statement_end_offset 
            WHEN -1 
            THEN DATALENGTH(h.text)
            ELSE r.statement_end_offset
            END - r.statement_start_offset) / 2) + 1
    ),
    [cpu_diff] = r.cpu_time - t.cpu_time, 
    [read_diff] = r.logical_reads - t.logical_reads,
    p.query_plan,
    r.wait_type,
    r.wait_time,
    r.last_wait_type,
    r.wait_resource,
    r.command,
    [database] = DB_NAME(r.database_id),
    r.blocking_session_id,
    r.granted_query_memory,
    r.session_id,
    r.reads,
    r.writes,
    r.row_count,
    s.[host_name],
    s.program_name,
    s.login_name
INTO #PERFSNAP
FROM sys.dm_exec_sessions s 
JOIN sys.dm_exec_requests r 
ON s.session_id = r.session_id
AND s.last_request_start_time = r.start_time
FULL JOIN #TEMP t 
ON t.session_id = s.session_id
CROSS APPLY sys.dm_exec_sql_text(r.sql_handle) h
CROSS APPLY sys.dm_exec_query_plan(r.plan_handle) p
ORDER BY 3 desc

DROP TABLE #TEMP

SELECT * FROM #PERFSNAP
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This is another variation of the above code snippet just without the cpu and read timing, and query plans.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;BEGIN
    -- Do not lock anything, and do not get held up by any locks.
    SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED

    -- What SQL Statements Are Currently Running?
    SELECT
        [spid] = session_Id,
        ecid,
        [database] = DB_NAME(sp.dbid),
        [user] = nt_username,
        [status] = er.status,
        [wait] = wait_type,
        [individual_query] = SUBSTRING(
            qt.text, 
            er.statement_start_offset / 2,
            (   CASE 
                WHEN er.statement_end_offset = -1
                THEN LEN(CONVERT(NVARCHAR(MAX), qt.text)) * 2
                ELSE er.statement_end_offset 
                END - er.statement_start_offset
            ) / 2
        ),
        [parent_query] = qt.text,
        program_name,
        hostname,
        nt_domain,
        start_time
    FROM sys.dm_exec_requests er
    JOIN sys.sysprocesses sp
    ON er.session_id = sp.spid
    CROSS APPLY sys.dm_exec_sql_text(er.sql_handle) qt
    WHERE session_Id &amp;gt; 50 -- ignore system spids
    AND session_Id NOT IN (@@SPID) -- ignore this current statement
    --AND session_Id IN ( 126 )
    --AND DB_NAME(sp.dbid) = &apos;&apos;
    ORDER BY 1, 2
END

--EXECUTE sp_who2 85
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;These snips will help track down different types of wait resources and their reasons.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;--KEY: 8:281474980315136 (4102a73ebc59)
--( Key Wait Resource )--
SELECT 
    [TableName] = o.name, 
    [IndexName] = i.name,
    [SchemaName] = SCHEMA_NAME(o.schema_id)
FROM sys.partitions p 
JOIN sys.objects o 
ON p.OBJECT_ID = o.OBJECT_ID 
JOIN sys.indexes i 
ON p.OBJECT_ID = i.OBJECT_ID
AND p.index_id = i.index_id 
WHERE p.hobt_id = 281474980315136

--( Page Wait Resource )--
SELECT DB_NAME(68)

-- select the database based on the output above statement then execute the below statement
DBCC traceon (3604)
GO
-- Database_id, file_id, page_id 
DBCC page (68, 1, 492478) 

--( Object Wait Resource )--
--OBJECT: 8:1954106002:0 
SELECT OBJECT_NAME(1954106002)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>SQL Server Shell Games</title><link>https://www.kichka.dev/posts/sql-shell-games/</link><guid isPermaLink="true">https://www.kichka.dev/posts/sql-shell-games/</guid><description>DELETE large amounts of data from a huge database table with this one simple trick</description><pubDate>Sat, 20 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;SQL Partition SWITCH Operation&lt;/h1&gt;
&lt;p&gt;With the power of the table partitioning &lt;code&gt;SWITCH&lt;/code&gt; operation you can swap table metadata pointers instantly.&lt;/p&gt;
&lt;p&gt;This can be used on any modern edition of sql server, even if table partioning is not supported. This is because under the hood every table is a single partition. With this you can quickly move data to an empty table and with some &quot;shell games&quot; instantly swap back to the old table.&lt;/p&gt;
&lt;p&gt;I have used this many times to remove billions of rows by selecting out only the few hundred thousand that I wanted to keep into a new table and swapping table partition pointers. The swap is instant and there is little to no downtime.&lt;/p&gt;
&lt;p&gt;Let&apos;s walk through an example of the process.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;-- create test set of tables
CREATE TABLE dbo.TEST1(id INT)
CREATE TABLE dbo.TEST2(id INT)

-- populate test1 with 10000 rows of data
DECLARE @i INT = 0
WHILE @i &amp;lt; 10000
BEGIN
    INSERT INTO dbo.TEST1(id)
    VALUES(@i)
    SELECT @i += 1
END

-- view current state of things
SELECT * FROM dbo.TEST1
SELECT * FROM dbo.TEST2

-- switch data to second table
ALTER TABLE dbo.TEST1
SWITCH TO dbo.TEST2

-- view current state of things
SELECT * FROM dbo.TEST1
SELECT * FROM dbo.TEST2
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Now that we have an idea of how this works, you can imagine that you have a table with billions of rows that you want to delete. DELETEs have to acquire a lock on the table at some level potentially preventing others from interacting with it. SELECTs however are much quicker and can be done in parallel without affecting others.&lt;/p&gt;
&lt;p&gt;Below, we select out just the handful of rows that we want, truncate the original table, then swap the table pointers.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;-- =========================
-- TRUNCATE TABLE SHELL GAME
-- =========================
-- move out set of data i want to keep
SELECT TOP 1000 * INTO dbo.TEMP_SHELL FROM dbo.TEST2

-- truncate orig table
TRUNCATE TABLE dbo.TEST2

-- move data metadata from shell to orig table
ALTER TABLE dbo.TEMP_SHELL
SWITCH TO dbo.TEST2

-- view current state of things
SELECT * FROM dbo.TEMP_SHELL
SELECT * FROM dbo.TEST2

-- remove shell table
DROP TABLE dbo.TEMP_SHELL

-- view current state of things
SELECT * FROM dbo.TEST1
SELECT * FROM dbo.TEST2

-- cleanup
DROP TABLE dbo.TEST1
DROP TABLE dbo.TEST2
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;This process has saved me countless hours of data processing and downtime.&lt;/p&gt;
&lt;p&gt;I have used it in ETLs, ad-hoc table cleanups, as well as to create a simple data archiving process that moves old data by quarter into another table for historical reporting keeping the primary table lean.&lt;/p&gt;
&lt;p&gt;This may not be such a concern now that Microsoft has opened up the table partitioning feature to all editions of SQL Server, but still useful for situations where the complexity of managing table partitioning outweighs the benifits.&lt;/p&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item><item><title>Start of a Journey</title><link>https://www.kichka.dev/posts/start-of-a-journey/</link><guid isPermaLink="true">https://www.kichka.dev/posts/start-of-a-journey/</guid><description>Today I embark on a new journey of writing down my thoughts and experiences in software development</description><pubDate>Mon, 15 Dec 2025 00:00:00 GMT</pubDate><content:encoded>&lt;h1&gt;A new Beginning&lt;/h1&gt;
&lt;p&gt;For a long time now I have been wanting to start writing my thoughts down on paper. I have a physical notebook where my thoughts and notes are haphazardly written which has this big downside of not being very searchable.&lt;/p&gt;
&lt;p&gt;So over the next couple of weeks I will be working on digitizing these notes and thoughts here.&lt;/p&gt;
&lt;p&gt;Come join me on this journey of inspiration lost and found.&lt;/p&gt;
&lt;p&gt;~ SK&lt;/p&gt;
</content:encoded></item></channel></rss>